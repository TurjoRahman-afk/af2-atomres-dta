# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.env
venv/
env/

# Model checkpoints & weights
*.pt
*.pth
*.ckpt
*.bin
*.safetensors
checkpoints/

# Large pretrained embeddings & contact maps (too large for git)
*.pkl

# Logs
*.log

# In-progress training logs (`_new` runs) — README is the source of truth for results
log/*_new*.csv

# Stray stdout redirects from background training runs
log/*.out

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Intermediate analysis arrays (regenerable from the scripts)
log/*.npz

# Local backups of large pickles (kept on disk, never committed)
*.pkl.bak

# analysis feature caches (regenerable, large)
code/analysis/.feats_*.npz
